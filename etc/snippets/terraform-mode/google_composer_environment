# -*- mode: snippet -*-
# name: google_composer_environment
# group: google
# key: g:cenv
# --

resource "google_composer_environment" "${1:resource name}" {
  name   = "${2:name}"
  region = "${3:region}"
  $0
}
